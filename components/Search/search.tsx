import React from "react";

/* const paramsFilter = `brand=${brandsFilter.join()}&category=${categoriesFilter.join()}&priceFrom=${price_from.replace(',', '').replace('.', '')}&priceTo=${price_to.replace(',', '').replace('.', '')}&page=${(page - 1) * 10}`; */
export const Search:React.FC = () => {
  return <input type="text" />
}